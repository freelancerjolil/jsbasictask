const friends = ["balam", "kalam", "salam", "jamal"];

// Adding Elements: You can add elements to an array using the push method (adds to the end)
// friends.push("abdul");
// console.log(friends);

// // Adding Elements: You can add elements to an array using the unshift method (adds to the beginning)
// friends.unshift("Hamid");
// console.log(friends);

// Removing Elements: You can remove elements using the pop method (removes the last element)
// friends.pop("jamal");
// console.log(friends);

// Removing Elements: You can remove elements using the shift method (removes the first element)
// friends.shift("balam");
// console.log(friends);